//reusable block of code
//set of statement
//large program to small task
//receive value and process and give result

function getSimpleInterest(p, r, y) {
  return (p * r * y) / 100;
}
let result = getSimpleInterest(1000, 10, 2);
let result2 = getSimpleInterest(2000, 10, 2);
console.log(result);
console.log(result2);
